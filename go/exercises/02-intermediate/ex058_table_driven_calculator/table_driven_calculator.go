// Package tabledrivencalculator — Exercise 058 (intermediate).
// Goal:   Calculate(op string, a, b float64) applies "add", "sub", "mul",
//         or "div" to a and b, returning an error for unknown operators or
//         division by zero.
// Drills: table-driven tests, error handling, switch statements.
package tabledrivencalculator

// Calculate applies the named operator ("add", "sub", "mul", "div") to a and
// b. It returns an error for an unknown operator or division by zero.
func Calculate(op string, a, b float64) (float64, error) {
	panic("TODO: implement Calculate")
}
