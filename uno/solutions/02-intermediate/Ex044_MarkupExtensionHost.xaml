<UserControl
    x:Class="FeWoLearning.Uno.Exercises.Intermediate.Ex044_MarkupExtensionHost"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FeWoLearning.Uno.Exercises.Intermediate">

    <StackPanel x:Name="Root">
        <!-- No properties set, so the extension's own defaults decide: 8 * 1. -->
        <Border x:Name="Default" Height="10" Width="{local:Ex044_MarkupExtension}" />

        <!-- Multiplier=3 is a property assignment on a fresh extension instance. -->
        <Border x:Name="Scaled" Height="10" Width="{local:Ex044_MarkupExtension Multiplier=3}" />
    </StackPanel>

</UserControl>
