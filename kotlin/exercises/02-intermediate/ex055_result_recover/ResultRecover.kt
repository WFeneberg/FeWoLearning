package fewolearning.exercises.intermediate.ex055_result_recover

/*
Exercise 055 - Result recover (intermediate).

Goal:   Recover a failed Result with a fallback value using recover.
Drills: recovering from failures.
*/
fun <T> Result<T>.recoverWithDefault(default: T): T {
    TODO()
}
