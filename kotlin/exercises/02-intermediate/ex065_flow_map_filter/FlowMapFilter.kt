package fewolearning.exercises.intermediate.ex065_flow_map_filter

import kotlinx.coroutines.flow.Flow

/*
Exercise 065 - Flow map/filter (intermediate).

Goal:   Transform a cold flow of numbers into a flow of even squares.
Drills: cold flows, transformations.
*/
fun evenSquaresFlow(source: Flow<Int>): Flow<Int> {
    TODO()
}
