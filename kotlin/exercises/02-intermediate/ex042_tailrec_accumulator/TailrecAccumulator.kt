package fewolearning.exercises.intermediate.ex042_tailrec_accumulator

/*
Exercise 042 - Tailrec accumulator (intermediate).

Goal:   Sum a list of numbers using tail recursion instead of a loop.
Drills: tail recursion, accumulator threading.
*/
tailrec fun sumTailrec(numbers: List<Int>, accumulator: Int = 0): Int {
    TODO()
}
