package fewolearning.exercises.intermediate.ex040_sequence_pipeline

/*
Exercise 040 - Sequence pipeline (intermediate).

Goal:   Chain filter/map/take lazily and only materialize the final result.
Drills: lazy multi-step transformations.
*/
fun firstEvenSquares(numbers: List<Int>, count: Int): List<Int> {
    TODO()
}
