package fewolearning.exercises.intermediate.ex041_inline_higher_order

/*
Exercise 041 - Inline higher-order function (intermediate).

Goal:   Run a block and return null instead of propagating its exception.
Drills: inline, non-local returns.
*/
inline fun <T> runOrNull(block: () -> T): T? {
    TODO()
}
