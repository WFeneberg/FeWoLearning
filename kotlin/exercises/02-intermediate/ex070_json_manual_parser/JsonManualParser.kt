package fewolearning.exercises.intermediate.ex070_json_manual_parser

/*
Exercise 070 - Manual JSON parser (intermediate).

Goal:   Parse a flat, string-valued JSON object into a map, without a library.
Drills: parsing, validation, recoverable errors.
*/
fun parseFlatJsonObject(json: String): Map<String, String> {
    TODO()
}
