package fewolearning.exercises.intermediate.ex069_test_dispatcher_time

/*
Exercise 069 - Test dispatcher virtual time (intermediate).

Goal:   Delay for a duration and return a value, testable under virtual time.
Drills: virtual time, coroutine test scheduler.
*/
suspend fun delayedValue(delayMillis: Long, value: Int): Int {
    TODO()
}
