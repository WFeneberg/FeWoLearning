package fewolearning.exercises.advanced.ex073_flow_debounce_search

import kotlinx.coroutines.flow.Flow

/*
Exercise 073 - Flow debounce search (advanced).

Goal:   Debounce a stream of search queries so only the settled query is used.
Drills: debounce, cancellation, latest results.
*/
fun debouncedQueries(queries: Flow<String>, debounceMillis: Long): Flow<String> {
    TODO()
}
