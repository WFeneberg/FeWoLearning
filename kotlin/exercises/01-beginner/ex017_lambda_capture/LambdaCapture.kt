package fewolearning.exercises.beginner.ex017_lambda_capture

/*
Exercise 017 - Lambda capture (beginner).

Goal:   Build a counter lambda that captures and increments its own state.
Drills: closures, captured state.
*/
fun makeCounter(): () -> Int {
    TODO()
}
