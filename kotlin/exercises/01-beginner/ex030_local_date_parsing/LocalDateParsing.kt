package fewolearning.exercises.beginner.ex030_local_date_parsing

import java.time.LocalDate

/*
Exercise 030 - LocalDate parsing (beginner).

Goal:   Parse an ISO date string and compute the number of days until another date.
Drills: Java time interop, parsing dates.
*/
fun parseIsoDate(isoDate: String): LocalDate {
    TODO()
}

fun daysUntil(start: LocalDate, end: LocalDate): Long {
    TODO()
}
