package fewolearning.exercises.beginner.ex031_big_decimal_money

import java.math.BigDecimal

/*
Exercise 031 - BigDecimal money (beginner).

Goal:   Add monetary amounts and round the result to whole cents.
Drills: BigDecimal, precision, rounding.
*/
fun add(first: BigDecimal, second: BigDecimal): BigDecimal {
    TODO()
}

fun roundToCents(amount: BigDecimal): BigDecimal {
    TODO()
}
