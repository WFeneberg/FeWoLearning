package fewolearning.exercises.beginner.ex018_scope_let_run

/*
Exercise 018 - Scope functions let/run (beginner).

Goal:   Transform a nullable input with let and compute a derived value with run.
Drills: let, run, temporary scopes.
*/
fun describeOrDefault(value: String?, default: String): String {
    TODO()
}

fun computeArea(width: Int, height: Int): Int {
    TODO()
}
