package fewolearning.exercises.beginner.ex003_ranges_and_when

/*
Exercise 003 - Ranges and when (beginner).

Goal:   Classify a score into a letter grade using ranges inside a when expression.
Drills: ranges, when, exhaustiveness.
*/
fun classify(score: Int): String {
    TODO()
}

fun isInRange(value: Int, range: IntRange): Boolean {
    TODO()
}
