package fewolearning.exercises.beginner.ex028_regex_validation

/*
Exercise 028 - Regex validation (beginner).

Goal:   Validate an email-like string and destructure its user/domain parts.
Drills: regular expressions, destructuring matches.
*/
fun isValidEmail(candidate: String): Boolean {
    TODO()
}

fun userAndDomain(email: String): Pair<String, String>? {
    TODO()
}
