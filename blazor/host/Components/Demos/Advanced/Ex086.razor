@page "/advanced/086"
@rendermode InteractiveServer

<h1>Exercise 086 - RenderModeStaticSsr</h1>

<p>Reload and watch the first paint: the prerender pass shows a real form, then the
   circuit attaches and it becomes a button. The console line comes from the interop
   call, which the static pass must not make.</p>

<Ex086_RenderModeStaticSsr />
