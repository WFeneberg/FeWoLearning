@* Exercise 042 - Nested Model Validation (intermediate).
   Goal:   Validate a model whose complex Address property has its own required field.
   Drills: nested-object validation, DataAnnotationsValidator, ValidationSummary.
   Passes: dotnet test --filter FullyQualifiedName~Ex042_ *@
@* TODO: render <EditForm Model="Model" OnValidSubmit="HandleValid" OnInvalidSubmit="HandleInvalid">
   containing <DataAnnotationsValidator />, <div id="errors"><ValidationSummary /></div>,
   an <InputText id="name" @bind-Value="Model.Name" />, an <InputText id="city"
   @bind-Value="Model.Address.City" />, and a submit button #submit, plus
   <span id="counts">@ValidCount/@InvalidCount</span>. ContactModel recurses into its
   own Address by hand (see ContactModel's comment - there is no supported
   [ValidateComplexType] in .NET 10) and that nested check only runs once Name and Age
   already pass on their own: a missing Name short-circuits with just "Name is
   required" and never reaches the City check at all. *@

@code {
    [Parameter, EditorRequired] public ContactModel Model { get; set; } = default!;

    public int ValidCount { get; private set; }
    public int InvalidCount { get; private set; }

    protected override void OnParametersSet()
        => throw new NotImplementedException("TODO: Ex042 - validate the nested address too");
}
