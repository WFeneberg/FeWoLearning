@* Exercise 018 - OnParametersSet (beginner).
   Goal:   Recompute a slug from Title on every parameter change.
   Drills: OnParametersSet runs on every parameter change, unlike
           OnInitialized (ex017) which runs only once.
   Passes: dotnet test --filter FullyQualifiedName~Ex018_ *@
<span id="slug">@_slug</span>

@code {
    [Parameter] public string Title { get; set; } = "";

    private string _slug = "";

    // TODO: set _slug from Title on every parameter change: lower-case, and
    // collapse each run of one or more non-alphanumeric characters (not just
    // spaces) into a single '-'. A leading run is dropped entirely (no
    // leading '-'); a trailing '-' is trimmed.
    protected override void OnParametersSet() => throw new NotImplementedException("TODO: Ex018 - recompute the slug on every parameter change");
}
