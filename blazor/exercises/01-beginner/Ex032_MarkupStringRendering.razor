@* Exercise 032 - MarkupString Rendering (beginner).
   Goal:   Render HTML raw only when explicitly allowed; otherwise show it as
           visible, escaped text.
   Drills: MarkupString, opting out of Blazor's default HTML-encoding.
   Passes: dotnet test --filter FullyQualifiedName~Ex032_ *@
@* TODO: render <div id="rich"> containing Html. When AllowHtml is true, Html must
   be interpreted as real HTML (e.g. a "<b>" in it becomes an actual bold element).
   When AllowHtml is false, Html must appear as plain, visible text - any markup
   characters in it stay literal, not interpreted. *@

@code {
    [Parameter] public string Html { get; set; } = "";
    [Parameter] public bool AllowHtml { get; set; }

    protected override void OnParametersSet()
        => throw new NotImplementedException("TODO: Ex032 - render raw markup only when it is allowed");
}
