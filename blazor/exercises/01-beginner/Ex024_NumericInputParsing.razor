@* Exercise 024 - Numeric Input Parsing (beginner).
   Goal:   Bind a numeric input to a local int field and show its total.
   Drills: @bind on an int field, @bind's own rejection of unparsable input.
   Passes: dotnet test --filter FullyQualifiedName~Ex024_ *@
@* TODO: render:
     <input id="qty" type="number" ... />
     <span id="total">...</span>
   Two-way bind the input to _quantity with @bind. Show _quantity * UnitPrice
   in #total, formatted with ToString("0.##", CultureInfo.InvariantCulture) -
   so a whole total shows as "0" or "7", not "0.0" or "7.0". Unparsable input
   must leave _quantity unchanged - that is @bind's own rejection behaviour,
   not something to hand-code. *@

@code {
    [Parameter] public decimal UnitPrice { get; set; }

    private int _quantity;

    protected override void OnParametersSet()
        => throw new NotImplementedException("TODO: Ex024 - bind the quantity and show the total");
}
