// Exercise 080 - CustomScrollView & slivers (advanced).
//
// Goal:   Build a scrollable list of items using CustomScrollView with a
//         SliverAppBar and a SliverList.
// Drills: CustomScrollView, SliverAppBar, SliverList, SliverChildBuilderDelegate.
// Passes: when the built widget lets a caller scroll from the first item to
//         a later item that wasn't initially visible.

import 'package:flutter/material.dart';

Widget buildItemListApp(List<String> items) {
  throw UnimplementedError('TODO');
}
