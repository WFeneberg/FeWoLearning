// Exercise 029 - RegExp basics (beginner).
//
// Goal:   Validate a simple email shape, and mask every digit in a string
//         with '#'.
// Drills: RegExp, RegExp.hasMatch, String.replaceAll with a pattern.
// Passes: when isValidEmail() rejects obviously malformed input, and
//         maskDigits() replaces every digit (and only digits) with '#'.

bool isValidEmail(String input) {
  throw UnimplementedError('TODO');
}

String maskDigits(String input) {
  throw UnimplementedError('TODO');
}
