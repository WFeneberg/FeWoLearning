// Exercise 028 - string manipulation (beginner).
//
// Goal:   Split a comma-separated line into trimmed fields, and left-pad a
//         numeric id to 4 digits with leading zeros.
// Drills: String.split, String.trim, String.padLeft.
// Passes: when splitAndTrim() trims whitespace around every field, and
//         padId() always returns exactly 4 characters.

List<String> splitAndTrim(String csvLine) {
  throw UnimplementedError('TODO');
}

String padId(int id) {
  throw UnimplementedError('TODO');
}
