// Exercise 020 - cascade notation (beginner).
//
// Goal:   Build a small text report (title, underline, then each line)
//         using a single StringBuffer composed with cascades.
// Drills: cascade operator (`..`), method chaining without repeating the
//         receiver.
// Passes: when buildReport() underlines the title with dashes matching its
//         length, then appends each line, all via one cascaded
//         StringBuffer.

String buildReport(String title, List<String> lines) {
  throw UnimplementedError('TODO');
}
