// Exercise 002 - string templates & multiline strings (beginner).
//
// Goal:   Build a greeting from a name and age using string interpolation,
//         and format a two-line receipt entry using a multiline string.
// Drills: string interpolation (`$name`, `${expr}`), multiline strings.
// Passes: when greet() and receiptLine() return exactly the expected text.

String greet(String name, int age) {
  throw UnimplementedError('TODO');
}

String receiptLine(String item, int quantity, double unitPrice) {
  throw UnimplementedError('TODO');
}
