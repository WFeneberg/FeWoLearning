// Exercise 014 - enums (beginner).
//
// Goal:   Return the opposite compass direction for a given direction.
// Drills: enums, enum values, exhaustive `switch`.
// Passes: when opposite() maps north<->south and east<->west.

enum Direction { north, east, south, west }

Direction opposite(Direction direction) {
  throw UnimplementedError('TODO');
}
