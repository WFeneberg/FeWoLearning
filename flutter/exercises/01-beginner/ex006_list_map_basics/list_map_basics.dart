// Exercise 006 - List/Map literals & indexing (beginner).
//
// Goal:   Look up the second item of a list, and the price of an item in a
//         map with a safe fallback when the item is unknown.
// Drills: List/Map literals, indexing (`list[i]`, `map[key]`).
// Passes: when secondItem() returns the item at index 1, and priceFor()
//         returns 0 for a key that isn't in the map instead of throwing.

String secondItem(List<String> items) {
  throw UnimplementedError('TODO');
}

int priceFor(Map<String, int> prices, String item) {
  throw UnimplementedError('TODO');
}
