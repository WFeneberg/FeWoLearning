// Exercise 003 - null safety basics (beginner).
//
// Goal:   Return a nickname if one is present, otherwise fall back to the
//         full name; and apply a "light" default theme when none is set.
// Drills: nullable types (String?), null-coalescing (??), null-aware
//         assignment (??=).
// Passes: when displayName() returns the nickname whenever it is non-null,
//         otherwise the full name, and applyDefaultTheme() returns the
//         given theme unchanged when set, otherwise "light".

String displayName(String? nickname, String fullName) {
  throw UnimplementedError('TODO');
}

String applyDefaultTheme(String? currentTheme) {
  throw UnimplementedError('TODO');
}
