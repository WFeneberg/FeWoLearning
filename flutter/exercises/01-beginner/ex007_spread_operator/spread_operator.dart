// Exercise 007 - spread operator (beginner).
//
// Goal:   Combine two lists into one, and combine a base list with an
//         optional (possibly null) list of extras.
// Drills: spread (`...`), null-aware spread (`...?`).
// Passes: when combine() concatenates both lists in order, and
//         combineWithExtras() ignores a null extras list instead of
//         throwing.

List<int> combine(List<int> a, List<int> b) {
  throw UnimplementedError('TODO');
}

List<int> combineWithExtras(List<int> base, List<int>? extras) {
  throw UnimplementedError('TODO');
}
