// Exercise 005 - switch expressions & records (beginner).
//
// Goal:   Classify an (x, y) coordinate record as "origin", "on-axis", or
//         "quadrant" using a Dart 3 switch expression over record patterns.
// Drills: records ((int, int)), switch expressions, pattern matching,
//         pattern guards (`when`).
// Passes: when classify() distinguishes the origin, either axis, and the
//         open-quadrant case using pattern guards rather than nested ifs.

String classify((int, int) point) {
  throw UnimplementedError('TODO');
}
