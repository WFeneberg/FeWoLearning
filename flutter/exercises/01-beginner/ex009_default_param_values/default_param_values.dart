// Exercise 009 - default parameter values (beginner).
//
// Goal:   Repeat a string a number of times, joined by a separator, using
//         optional positional parameters with defaults.
// Drills: optional positional parameters (`[...]`), default values.
// Passes: when repeatJoin() defaults to a single copy joined by ", " and
//         honors overrides for either parameter.

String repeatJoin(String value, [int times = 1, String separator = ', ']) {
  throw UnimplementedError('TODO');
}
