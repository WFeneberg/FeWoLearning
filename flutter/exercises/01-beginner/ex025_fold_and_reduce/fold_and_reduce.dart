// Exercise 025 - fold vs reduce (beginner).
//
// Goal:   Sum a list of numbers with fold, find the maximum with reduce, and
//         join a list of words into a comma-separated string with fold.
// Drills: Iterable.fold, Iterable.reduce, accumulator patterns.
// Passes: when sumWithFold()/maxWithReduce()/joinWithFold() match plain
//         iteration, and maxWithReduce() propagates reduce's StateError on
//         an empty list rather than being special-cased away.

int sumWithFold(List<int> numbers) {
  throw UnimplementedError('TODO');
}

int maxWithReduce(List<int> numbers) {
  throw UnimplementedError('TODO');
}

String joinWithFold(List<String> words) {
  throw UnimplementedError('TODO');
}
