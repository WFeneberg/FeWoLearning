// Exercise 038 - Future.wait (intermediate).
//
// Goal:   Double every value in a list concurrently and sum the results.
// Drills: Future.wait, parallel futures.
// Passes: when sumInParallel() runs every doubling concurrently (via
//         Future.wait) rather than awaiting each one sequentially in a loop.

Future<int> sumInParallel(List<int> values) {
  throw UnimplementedError('TODO');
}
