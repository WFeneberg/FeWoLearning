// Exercise 048 - destructuring patterns & records (intermediate).
//
// Goal:   Format a list of (name, score) records as "name: score" lines.
// Drills: records ((String, int)), destructuring in a for-in pattern.
// Passes: when formatScores() destructures each record directly in the
//         for-in loop rather than indexing into it.

List<String> formatScores(List<(String, int)> entries) {
  throw UnimplementedError('TODO');
}
