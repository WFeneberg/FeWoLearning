// Exercise 040 - Stream.map/where (intermediate).
//
// Goal:   Keep only even numbers from a stream and square each one.
// Drills: Stream.where, Stream.map, chaining stream transformations.
// Passes: when evenSquares() yields only the squares of even source values,
//         in order.

Stream<int> evenSquares(Stream<int> source) {
  throw UnimplementedError('TODO');
}
