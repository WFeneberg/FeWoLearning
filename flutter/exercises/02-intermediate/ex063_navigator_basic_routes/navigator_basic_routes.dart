// Exercise 063 - Navigator basic routes (intermediate).
//
// Goal:   Build a home screen that navigates to a details screen via a
//         named route, and a details screen that pops back.
// Drills: Navigator.of(context).pushNamed, Navigator.of(context).pop,
//         named routes.
// Passes: when tapping the home screen's button navigates to the details
//         screen, and tapping the details screen's button returns home.

import 'package:flutter/material.dart';

class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError('TODO');
  }
}

class DetailsScreen extends StatelessWidget {
  const DetailsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError('TODO');
  }
}
