namespace FeWoLearning.Exercises.Beginner;

// Exercise 013 — AgeCalculator (beginner).
// Goal:   Compute a person's age in whole years given a birth date and a
//         reference ("today") date. Handle leap-year birthdays (Feb 29) and
//         cases where the reference date falls before/after the birthday.
// Drills: DateTime arithmetic, date comparisons, leap years.
public static class AgeCalculator
{
    public static int GetAge(DateTime birthDate, DateTime referenceDate) => throw new NotImplementedException();
}
