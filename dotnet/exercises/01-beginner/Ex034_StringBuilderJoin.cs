namespace FeWoLearning.Exercises.Beginner;

// Exercise 034 — StringBuilderJoin (beginner).
// Goal:   Build a single CSV line from an array of fields using StringBuilder.
//         Fields containing a comma must be wrapped in double quotes so the
//         resulting line stays a valid single CSV row, e.g.
//         ["a", "b,c", "d"] -> "a,\"b,c\",d".
// Drills: StringBuilder usage, string escaping, iteration.
public static class StringBuilderJoin
{
    public static string BuildCsvLine(string[] fields) => throw new NotImplementedException();
}
