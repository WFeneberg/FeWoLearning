namespace FeWoLearning.Exercises.Beginner;

// Exercise 016 — Fibonacci Sequence (beginner).
// Goal:   Implement an iterator method (using `yield return`) that lazily
//         produces the first `count` numbers of the Fibonacci sequence,
//         starting 0, 1, 1, 2, 3, 5, 8, ...
// Drills: custom IEnumerable<T> via yield, iterator methods, deferred execution.
public static class FibonacciSequence
{
    public static IEnumerable<long> Generate(int count) => throw new NotImplementedException();
}
