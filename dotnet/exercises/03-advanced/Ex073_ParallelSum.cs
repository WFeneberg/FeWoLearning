namespace FeWoLearning.Exercises.Advanced;

// Exercise 073 — Parallel array sum (advanced).
// Goal:   Compute the sum of a large int array using Parallel.ForEach with
//         thread-safe accumulation, matching the sequential sum exactly.
// Drills: Parallel.ForEach, thread-local partitioning, Interlocked accumulation.
public static class ParallelSum
{
    public static long Sum(int[] values) => throw new NotImplementedException();
}
