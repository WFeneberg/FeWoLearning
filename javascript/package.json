{
  "name": "fewolearning-javascript",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:one": "vitest run -t",
    "test:solutions": "vitest run -c vitest.solutions.config.js"
  },
  "devDependencies": {
    "vitest": "5.0.1"
  }
}
